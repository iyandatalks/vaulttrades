"use client";
import { useState } from "react";
import { STRATEGY_LIST, type StrategyId } from "../../lib/strategies";
type Timeframe="M1"|"M5"|"M10"|"M15"|"M30"|"H1"|"H4"|"D1"; type Signal="BUY"|"SELL"|"BUY DEVELOPING"|"SELL DEVELOPING"|"WAITING"|"NO TRADE";
type Result={tradeSignal?:{direction:Signal;confidence:number;entry:number|null;stopLoss:number|null;risk:number|null;tp1:number|null;tp2:number|null;finalTp:number|null;invalidation:string};decisionReason?:string;marketState?:string;setup?:string;confirmedConditions?:string[];missingConditions?:string[];error?:string};
const TIMEFRAMES:Timeframe[]=["M1","M5","M10","M15","M30","H1","H4","D1"]; const price=(v:number|null|undefined)=>v==null||!Number.isFinite(v)?"WAIT":v.toLocaleString(undefined,{maximumFractionDigits:5});
export default function AnalyzerPage(){const[strategy,setStrategy]=useState<StrategyId>("killZone");const[timeframe,setTimeframe]=useState<Timeframe>("M5");const[file,setFile]=useState<File|null>(null);const[preview,setPreview]=useState("");const[result,setResult]=useState<Result|null>(null);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const selected=STRATEGY_LIST.find(x=>x.rules.id===strategy)?.rules;const analyze=async()=>{if(!file){setError("Please upload a TradingView chart first.");return;}setLoading(true);setError("");setResult(null);try{const form=new FormData();form.append("image",file);form.append("strategy",strategy);form.append("timeframe",timeframe);const res=await fetch("/api/analyze-v2",{method:"POST",body:form});const data=await res.json();if(!res.ok)throw new Error(data.error||"Unable to analyze the chart.");setResult(data)}catch(e){setError(e instanceof Error?e.message:"Unable to analyze the chart.")}finally{setLoading(false)}};return <main className="shell"><header className="header"><div className="brand-block"><img src="/vaulttrades-logo.png" alt="VaultTrades" className="logo"/><div className="tagline">Built by Traders.</div><div className="slogan">Focus, discipline, consistency.</div></div><div className="badge">ANALYZER</div></header><section className="card"><div className="section-label">STRATEGY LIBRARY</div><h1 className="title">Analyze by Strategy</h1><p className="muted">The Analyzer applies one independent strategy at a time. It explains why a trade is valid or why no trade should be taken.</p><div style={{display:"grid",gap:10,marginTop:18}}>{STRATEGY_LIST.map(({rules})=><button key={rules.id} type="button" className={`strategy ${strategy===rules.id?"active":""}`} disabled={loading} onClick={()=>{setStrategy(rules.id);setResult(null)}}><strong>{rules.name}</strong><span className="muted">{rules.description}</span></button>)}</div>{selected&&<div className="condition-box" style={{marginTop:16}}><strong>Required sequence</strong><p className="muted">{selected.sequence.join(" → ")}</p></div>}</section><section className="card"><div className="section-label">CHART ANALYZER</div><h2 className="title">Select Timeframe</h2><div className="timeframe-grid">{TIMEFRAMES.map(tf=><button key={tf} type="button" className={`timeframe-button ${timeframe===tf?"selected":""}`} onClick={()=>setTimeframe(tf)} disabled={loading}>{tf}</button>)}</div><label className="upload" style={{marginTop:18}}><input type="file" accept="image/png,image/jpeg,image/webp" hidden disabled={loading} onChange={e=>{const f=e.target.files?.[0];if(!f)return;setFile(f);setPreview(URL.createObjectURL(f));setResult(null);setError("")}}/>{preview?<><strong>{file?.name}</strong><img src={preview} alt="Trading chart preview" className="preview"/></>:<><strong>Upload TradingView Chart</strong><div className="muted">PNG, JPG or WebP</div></>}</label><div className="actions"><button className="primary" type="button" disabled={!file||loading} onClick={()=>void analyze()}>{loading?"Analyzing Chart...":"Analyze Chart"}</button><button className="secondary" type="button" disabled={loading} onClick={()=>{setFile(null);setPreview("");setResult(null);setError("")}}>Clear</button></div>{error&&<div className="error-box"><strong>Analysis Error</strong><p className="muted">{error}</p></div>}</section>{result?.tradeSignal&&<><section className="card execution-card"><div className="section-label">TRADE DECISION</div><div className="execution-header"><div><div className="execution-label">DECISION</div><div className="execution-direction">{result.tradeSignal.direction}</div></div><div className="confidence-box"><span>CONFIDENCE</span><strong>{result.tradeSignal.confidence}%</strong></div></div><div className="execution-grid"><div className="execution-item"><span>ENTRY</span><strong>{price(result.tradeSignal.entry)}</strong></div><div className="execution-item"><span>STOP LOSS</span><strong>{price(result.tradeSignal.stopLoss)}</strong></div><div className="execution-item"><span>TP1</span><strong>{price(result.tradeSignal.tp1)}</strong></div><div className="execution-item"><span>TP2</span><strong>{price(result.tradeSignal.tp2)}</strong></div><div className="execution-item"><span>FINAL TP</span><strong>{price(result.tradeSignal.finalTp)}</strong></div></div><div className="condition-box" style={{marginTop:16}}><strong>Why this decision?</strong><p style={{whiteSpace:"pre-wrap",lineHeight:1.7}}>{result.decisionReason||"The strategy result is shown from the conditions confirmed on the chart."}</p></div></section><section className="card"><div className="section-label">MARKET STATE</div><h2 className="title">What the Chart Is Telling You</h2><div className="condition-box"><strong>Market State</strong><p>{result.marketState||"Not established."}</p></div><div className="condition-box" style={{marginTop:12}}><strong>Setup</strong><p>{result.setup||"No completed setup established."}</p></div></section><section className="card"><div className="section-label">EDUCATIONAL BREAKDOWN</div><div className="condition-box"><strong>Confirmed Conditions</strong>{result.confirmedConditions?.length?<ul>{result.confirmedConditions.map(x=><li key={x}>✓ {x}</li>)}</ul>:<p className="muted">No required conditions are confirmed yet.</p>}</div><div className="condition-box" style={{marginTop:12}}><strong>Missing Conditions / What To Wait For</strong>{result.missingConditions?.length?<ul>{result.missingConditions.map(x=><li key={x}>• {x}</li>)}</ul>:<p className="muted">No missing conditions reported.</p>}</div><div className="condition-box" style={{marginTop:12}}><strong>Invalidation</strong><p>{result.tradeSignal.invalidation||"No invalidation supplied."}</p></div></section></>}</main>}
